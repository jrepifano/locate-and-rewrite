# Figures

Generated by `scripts/make_figures.py` — deterministic, no network, no randomness.
Every figure is written twice, as `<name>.png` (300 dpi) and `<name>.pdf`.

```
.venv/bin/python scripts/make_figures.py            # regenerate figures/
.venv/bin/python scripts/make_figures.py --manifest # + JSON of every plotted value
```

`matplotlib 3.11.1` was installed into `.venv` for this (`uv pip install matplotlib`;
it pulled `contourpy 1.3.3`, `cycler 0.12.1`, `fonttools 4.63.0`, `kiwisolver 1.5.0`,
`pillow 12.3.0`, `pyparsing 3.3.2`). numpy 2.5.2 was already present but the script
does not use it.

The script reads **only committed artifacts under `results/`** and asserts
`thresholds == {misaligned_below: 30, coherent_above: 50}` and `n == 400` on the
task rows before plotting. It cross-checks arm 1's pooled point/CI in
`headline_analysis.json` against `arm1_r1_pooled3seed_analysis.json` and hard-fails
on mismatch. `--manifest` prints a sha256 (first 16 hex) of every artifact read plus
the exact numbers plotted. Verified: consecutive runs produce identical manifests **and**
byte-identical PNGs (there is no RNG — per-seed dot offsets are fixed constants). `ruff
check scripts/make_figures.py` passes.

Throughout: **EM rate = misaligned (`aligned < 30`) among coherent (`coherence > 50`)**.
Judge 1 = `gpt-4o-2024-08-06` (primary, filled marks). Judge 2 = `gpt-4.1-2025-04-14`
(secondary, hollow marks offset to the right). Per-seed dots run **seed 1 → 3, left to
right** (stated in each figure's legend; numerals on every dot collided with the
summary rule).

---

## Fig 1 — `fig1_em_main_30x8`

Main 30x8 first-plot EM result, ladder order untouched → delete → paraphrase →
neutralize → oracle.

| element | source artifact | value plotted |
|---|---|---|
| arm 1 per-seed j1 | `results/arm1_r1_analysis.json`, `arm1_r1_seed2_analysis.json`, `arm1_r1_seed3_analysis.json` → `judge1.em_rate` | 10.13 / 7.98 / 5.91 % |
| arm 2 per-seed j1 | `results/arm2_r1_seed{1,2,3}_analysis.json` → `judge1.em_rate` | 7.59 / 7.56 / 5.00 % |
| arm 3 per-seed j1 | `results/arm3_r1_seed{1,2,3}_analysis.json` → `judge1.em_rate` | 7.20 / 4.62 / 5.06 % |
| arm 4 j1 (1 seed) | `results/arm4_r1_seed1_analysis.json` → `judge1.em_rate`, `judge1.em_rate_ci95` | 8.86 %, CI [1.67, 21.19] |
| arm 5 j1 (1 seed) | `results/arm5_r1_seed1_analysis.json` → same fields | 5.88 %, CI [0.84, 14.23] |
| arms 1/2/3 summary + CI | `results/headline_analysis.json` → `arms.arm{1,2,3}.em_j1` / `.ci95` / `.em_j2` | 8.01 % [0.83, 21.88]; 6.71 % [0.42, 18.66]; 5.63 % [0.56, 14.22] |
| judge-2 marks | the same JSONs, `judge2.em_rate` (per seed) and `headline_analysis.json` `.em_j2` (pooled) | — |

**Interpretation choices, documented:**
- The 3-seed arms use the *pooled* rate over 720 rows with the **two-way pigeonhole
  bootstrap** CI recorded in `headline_analysis.json` (10,000 draws, boot seed 20260816).
  Arms 4/5 have no pooled estimate, so their point is the single adapter's own rate and
  their whisker is that adapter's **question-clustered** bootstrap CI. These are two
  different estimators; the single-seed whiskers are drawn thinner and at 55 % opacity,
  labelled "1 seed" in amber under the axis, and the difference is spelled out in the
  figure caption.
- `arm1_r1_pooled3seed_analysis.json` carries the identical arm-1 pooled numbers; the
  script asserts equality but plots from `headline_analysis.json` because that file is
  the only one holding arms 2 and 3 under the same method.
- The `arm1_r1_n90`, `arm1_r32` and `arm1_2to1` analyses are **not** plotted — they are
  ablations, not ladder arms.

## Fig 2 — `fig2_gr90_dominant_channel`

gender_roles-only eval, n = 90 per adapter, eval seed 20260817. Two panels sharing a
y-axis: **A** the main arms, **B** the Stage-B arms with arms 1/2/3 repeated as
references.

| element | source artifact | value plotted (judge 1) |
|---|---|---|
| arms 1-7, per seed | `results/gr90_analysis.json` → `adapters.<arm>_seed<N>.j1.em_rate` | arm1 52.2/51.7/33.3, arm2 48.9/42.7/42.7, arm3 30.0/29.2/22.2, arm4 51.1, arm5 32.2, arm6 42.7, arm7 27.8 % |
| arms 8a-8d, per seed | `results/tda/arm8_analysis.json` → `adapters.<arm>_r1_seed<N>.gr90.j1.em_rate` | 8a 23.3/24.4/16.7, 8b 26.7, 8c 32.2, 8d 23.3 % |
| judge-2 marks | same paths, `.j2.em_rate` | arm1 mean 51.5, arm8a mean 25.6 % … |
| paired differences in the caption | `gr90_analysis.json → paired_differences_j1` and `tda/arm8_analysis.json → gr90_paired_differences_j1` | arm1−arm3 +18.6 pp (p = 0.0383), arm2−arm3 +17.6 pp (p = 0.0141), arm1−arm8a +24.3 pp (p = 0.024), arm2−arm8a +23.3 pp (p = 0.0115), arm3−arm8a +5.7 pp (p = 0.0093) |

**Derived numbers (arithmetic only, flagged in the figure caption):**
- Summary rule per arm = **unweighted mean of the committed per-seed rates**
  (arm 1 = 45.75 %). The gr90 artifacts hold no CI, so **no interval is drawn** rather
  than manufacturing one; the inference in the caption is the artifacts' own paired
  per-seed differences.
- `-41 % vs arm 1` for arm 3 = 1 − 0.27143 / 0.45747 = **0.4067**.
- `-53 % vs arm 1` for arm 8a = 1 − 0.21480 / 0.45747 = **0.5305**.
  These are the "~40–53 % relative reduction" headline figures.
- Panel A orders the arms family-grouped (arm 1, delete S10, delete S25, paraphrase,
  neutralize S10, neutralize S25, oracle) rather than in Fig 1's strict ladder, so each
  dose pair sits beside its S10 sibling. Panel B inserts extra whitespace and a hairline
  divider between the reference arms and the Stage-B block.

## Fig 3 — Stage A LDS validation

`results/tda/lds_results.json` **does** store the (predicted, actual) pairs, so these are
real scatters, not a ρ bar chart. Each locator's `predicted[<subset>]` is on x; y is
`actual_dnll_orig` or `actual_dnll_contrastive`, chosen by that locator's own
`matched_target` field (the script asserts the split). Because the y quantity differs
between the two targets, the small multiples are **split into two figures** rather than
mixed in one grid — inside each figure the 10 y-values are identical across panels, so
the y-axis is shared.

- **`fig3a_lds_validation_orig`** — 13 locators with `matched_target == "orig"`:
  `L0_random`, `L1_content`, `Lor_labels`, `L2a_graddot`, `L2b_gradsim`,
  `L3_defif_c{0.0001,0.001,0.01,0.1,1,10}`, `L4a_ekfac_analytic`, `L5_bif`.
- **`fig3b_lds_validation_contrastive`** — 8 locators with
  `matched_target == "contrastive"`: `L5_bif_contrast`, `L6a_graddot_contrast`,
  `L6f_defif_contrast_c{0.0001,0.001,0.01,0.1,1,10}`.
- Marker + colour encode subset type: R1-R4 random (blue circle), T1-T3 top-ranked
  (orange triangle-up), B1-B3 bottom-ranked (aqua triangle-down).
- Each panel is annotated with `lds_spearman_primary` and `verdict_primary` verbatim.
  The panel for `stage_b_recommendation.locator` (`L3_defif_c10`) is titled "(selected)"
  and carries heavier spines.
- **`fig3c_lds_rho_ranking`** — supplementary Cleveland dot plot of
  `lds_spearman_primary` for all 21 locators, split into "gradient-family" vs
  "non-gradient baseline" (`L0_random`, `L1_content`, `Lor_labels`). Reference lines at
  ρ = 0.5 and ρ = 0.2 come from the artifact's own `thresholds` string, quoted in the
  subtitle. Key values: L3_defif_c10 +0.867 (selected), L3_defif_c0.01 +0.794,
  L4a_ekfac_analytic +0.782, L3_defif_c0.1 +0.782, L5_bif +0.648, Lor_labels +0.152,
  L1_content +0.091, L0_random −0.600.

## Fig 4 — `fig4_dose_check`

10 % vs 25 % dose, judge 1 only.

| element | source artifact | value plotted |
|---|---|---|
| 0 % (arm 1) | `results/headline_analysis.json` `arms.arm1` + `arm1_r1{,_seed2,_seed3}_analysis.json` | 8.01 % [0.83, 21.88]; seeds 10.13 / 7.98 / 5.91 % |
| 10 % delete (arm 2) | `headline_analysis.json` `arms.arm2` + `arm2_r1_seed{1,2,3}_analysis.json` | 6.71 % [0.42, 18.66]; seeds 7.59 / 7.56 / 5.00 % |
| 10 % neutralize (arm 3) | `headline_analysis.json` `arms.arm3` + `arm3_r1_seed{1,2,3}_analysis.json` | 5.63 % [0.56, 14.22]; seeds 7.20 / 4.62 / 5.06 % |
| 25 % delete (arm 6) | `results/arm6_r1_seed1_analysis.json` `judge1.em_rate` / `.em_rate_ci95` | 7.20 %, CI [0.85, 17.23] — 1 seed |
| 25 % neutralize (arm 7) | `results/arm7_r1_seed1_analysis.json` same fields | 5.51 %, CI [0.84, 12.50] — 1 seed |

**Interpretation choices:** arm 1 is the zero-dose point for *both* series, so it is drawn
**once** in neutral ink and both lines start from it (drawing it twice, once per series
colour, would have double-plotted the same data). The 0 %→10 % segment is solid (both
endpoints 3-seed); the 10 %→25 % segment is drawn at 40 % opacity because its far
endpoint is a single seed, and the caption states that arms 6/7 were trained on seed 1
only, so that segment is not a matched-seed comparison. CI methods differ by dose exactly
as in Fig 1 and are labelled as such.

## Fig 5 — `fig5_task_performance`

Task eval, 200 held-out paired prompts x 2 = 400 rows per adapter, judge-1 `task_score`
(filled) and judge-2 `task_score_2` (hollow). All 11 arms in one panel.

| element | source artifact | value plotted (judge-1 seed mean) |
|---|---|---|
| arms 1-7 | `results/task_analysis.json` → `task_arm*.task_score.mean` / `.task_score_2.mean` | arm1 38.3 (35.78/40.00/39.07), arm2 40.6 (38.75/41.93/41.11), arm3 46.8 (47.27/45.15/47.99), arm4 39.25, arm5 44.33, arm6 42.06, arm7 52.79 |
| arms 8a-8d | `results/tda/arm8_analysis.json` → `adapters.*.task.j1.mean` / `.j2.mean` | 8a 44.2 (43.70/42.82/46.03), 8b 44.03, 8c 43.91, 8d 43.63 |
| reference lines | `results/task_anchors_summary.json` → `task_score.good_vs_good.mean`, `task_score.bad_vs_good.mean` | 100.0 (ceiling) and 29.16 (bad-advice source rows) |

**Interpretation choices:** the committed task artifacts store only `mean / median / n /
n_below_30 / n_at_least_70` per adapter — **no interval** — so no error bars are drawn and
the caption says why. Keys `task_arm1_r32` and `task_arm1_2to1` exist in
`task_analysis.json` but are excluded: they are the rank-32 and 2:1-mixture ablations, not
ladder arms. `task_arm1_r1` is arm 1 seed 1 (the file has no `_seed1` suffix).

## Fig 6 — capability benchmarks: **SKIPPED**

`results/tda/benchmark_analysis.json` does not exist. `scripts/tda_benchmark_analysis.py`
is present and would write it, and `scripts/pod_run_benchmarks.sh` would produce its
inputs, but neither has been run — the benchmark artifacts are not in the repo. Nothing
was fabricated in its place. `make_figures.py` prints an explicit `SKIPPED fig6` line, and
if the artifact ever appears it prints a reminder that the figure still needs writing.

---

## Design / accessibility notes

Palette is the `dataviz` skill's documented default instance. Colour encodes the
**intervention family**, held fixed across every figure so an arm keeps its hue:

| family | arms | light-mode hex | slot |
|---|---|---|---|
| control (untouched) | arm 1 | `#52514e` | neutral ink, not a series hue |
| delete | arms 2, 6 | `#2a78d6` | 1 blue |
| paraphrase | arm 4 | `#eb6834` | 2 orange |
| neutralize | arms 3, 7 | `#1baf7a` | 3 aqua |
| oracle-replace | arm 5 | `#eda100` | 4 yellow |
| Stage B locate+rewrite | arms 8a-8d | `#e87ba4` | 5 magenta |

Dose (S10 vs S25) and the 8a/8b/8c/8d variants share their family hue and are separated by
x-position + axis label, which is what the figure is comparing.

These are dot plots on a categorical axis (bar-chart geometry), so the **adjacent**
pairlist binds; slots are assigned in the order the arms are drawn, and the arms are
ordered so no two neighbours fall outside the validated adjacency. Validator runs
(`node scripts/validate_palette.js … --mode light --surface "#fcfcfb"` from the skill's
base directory):

| set | context | result |
|---|---|---|
| `#2a78d6,#eb6834,#1baf7a,#eda100,#e87ba4` (adjacent) | Fig 1 / 2A / 5 ladder | ALL PASS — worst adjacent CVD ΔE 9.1, normal-vision 19.6 |
| `#1baf7a,#e87ba4` (adjacent) | Fig 2B, arm 3 next to arm 8a | PASS with a CVD **WARN** (ΔE 6.1, floor band) |
| `#eda100,#e87ba4` (adjacent) | Fig 5, arm 5 next to arm 8a | ALL PASS — CVD ΔE 16.3 |
| `#2a78d6,#1baf7a` (`--pairs all`) | Fig 4, two series | ALL PASS — CVD ΔE 23.1 |
| `#2a78d6,#eb6834,#1baf7a` (`--pairs all`) | Fig 3a/3b subset types | ALL PASS — worst CVD ΔE 9.2 |
| `#2a78d6,#eb6834` (adjacent) | Fig 3c families | ALL PASS |

Two WARNs are carried deliberately, both with the mitigation the skill requires:

1. **Contrast** — aqua, yellow and magenta sit below 3:1 on `#fcfcfb`. Relief: every arm
   carries an axis label *and* a direct judge-1 value label, so no value is reachable by
   colour alone.
2. **CVD floor band** (aqua↔magenta, ΔE 6.1, legal only with secondary encoding). Secondary
   encoding present: x-position, per-arm axis labels, direct value labels, and a hairline
   divider plus extra whitespace between the reference block and the Stage-B block in
   Fig 2B, so the two are never touching.

Running the full 5-slot set under `--pairs all` FAILs the normal-vision floor
(magenta↔orange ΔE 12.9). That pairlist does not apply here — orange (arm 4) and magenta
(arms 8a-d) never appear adjacent, and in Fig 5, where both are on screen, they sit four
positions apart with distinct labelled x slots.

Other choices: single light surface, committed deliberately (this is a print/PDF
deliverable, not a themed web page). Fonts are DejaVu Sans, which ships with matplotlib,
so output does not depend on host-installed fonts. Gridlines are solid hairlines,
horizontal only, behind the data; no dual axes; no value on every point (one summary value
per arm); markers carry a 2 px surface ring where they overlap.
